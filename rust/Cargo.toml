[package]
name = "costcut-infer"
version = "0.1.0"
edition = "2021"
description = "CostCut Infer（Rust 版，性能优先——纯 std 推理运行时）"

[dependencies]
# 以下依赖已全部下载（cargo fetch 完成）——但本机缺 C 工具链（gcc.exe，zstd-sys 等 C 库编译受阻），
# 已回退纯 std 以恢复构建；C 工具链就绪后取消注释即可启用。
# anyhow = "1.0.104"
# candle-core = "0.11.0"
# memmap2 = "0.9.11"
# rayon = "1.12.0"
# regex = "1.13.1"
# serde_json = "1.0.151"
# tch = "0.24.0"
# thiserror = "2.0.20"
# tokenizers = "0.23.1"
